import React, {Component} from 'react'
import {
    Text,
    View,
    StatusBar,
    SafeAreaView,
    ScrollView,
    StyleSheet
} from 'react-native'

export default class FunctionPage extends Component<Props>  {
    render() {
        return (
            <View>首页</View>
        );
    }
}