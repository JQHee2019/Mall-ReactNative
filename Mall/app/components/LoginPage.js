import React, {Component} from 'react'
import {
    Text,
    View,
    StatusBar,
    SafeAreaView,
    ScrollView,
    StyleSheet
} from 'react-native'

/**
 * 登录页面
*/

export default class LoginPage extends Component<Props>  {
    render() {
        return (
            <View> 
                <Text>登录页面</Text>
            </View>
        );
    }
}