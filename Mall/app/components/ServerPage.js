import React, {Component} from 'react'
import {
    Text,
    View,
    StatusBar,
    SafeAreaView,
    ScrollView,
    StyleSheet
} from 'react-native'

export default class ServerPage extends Component<Props>  {
    render() {
        return (
            <View>客服</View>
        );
    }
}