import React, {Component} from 'react'
import {
    Text,
    View,
    StatusBar,
    SafeAreaView,
    ScrollView,
    StyleSheet
} from 'react-native'

/**
 * 客服页面
 */

export default class ServerPage extends Component<Props>  {
    render() {
        return (
            <View> 
                <Text>客服</Text>
            </View>
        );
    }
}