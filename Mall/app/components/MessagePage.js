import React, {Component} from 'react'
import {
    Text,
    View,
    StatusBar,
    SafeAreaView,
    ScrollView,
    StyleSheet
} from 'react-native'

export default class MessagePage extends Component<Props>  {
    render() {
        return (
            <View> 
             <Text>个人中心</Text>
            </View>
        );
    }
}