import React, {Component} from 'react'
import {
    Text,
    View,
    StatusBar,
    SafeAreaView,
    ScrollView,
    StyleSheet
} from 'react-native'

export default class MinePage extends Component<Props>  {
    render() {
        return (
            <View>个人中心</View>
        );
    }
}