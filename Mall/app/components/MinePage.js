import React, {Component} from 'react'
import {
    Text,
    View,
    StatusBar,
    SafeAreaView,
    ScrollView,
    StyleSheet
} from 'react-native'

/**
 * 我的页面
 */

export default class MinePage extends Component<Props>  {
    render() {
        return (
            <View> 
                <Text>个人中心</Text>
            </View>
        );
    }
}